## Routing configuration

Lua 代码通过 `get_route_configuration` 获取路由配置, 校验然后以 hash key 存储校验结果。

The Lua code obatins the routing configuration through `get_route_configuration`,verifies it and stores the verification result in a hash key
